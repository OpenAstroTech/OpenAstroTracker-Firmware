tabulate~=0.8.7
python-constraint~=1.4.0
# matrix_build.py also requires click, but platformio also requires click so we just let them pick the version
# use the latest version of platformio, same as what a user would install
platformio
